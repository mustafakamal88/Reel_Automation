export function CompetitorsPage() {
  return (
    <section className="page-section">
      <div className="empty-state">
        <div className="empty-icon">CP</div>
        <div className="empty-title">No competitor tracking configured.</div>
        <div className="empty-desc">Add real competitor accounts before this page shows public signal tracking.</div>
      </div>
    </section>
  );
}
